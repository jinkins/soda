export class Log {
    private id: string; 
    private type: string; // type of log (for )
    private subject:string;  // Log id
    private message:string; // message itself
    private when: Date; 
    private by: string; // uid of the updater
}
